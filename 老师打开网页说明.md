# 《转基因时光机》网页项目打开说明

老师您好，本项目是一个使用 React + Vite + TypeScript 制作的交互式网页，不需要后端，也不需要连接数据库。拿到整个项目文件夹后，可以按下面步骤打开。

## 方法一：开发预览方式打开

1. 先安装 Node.js  
   如果电脑还没有安装 Node.js，请先安装 Node.js LTS 版本。

2. 打开项目文件夹  
   进入文件夹：`transgenic-time-machine`

3. 在该文件夹中打开终端  
   Windows 可以在文件夹空白处右键，选择“在终端中打开”或“在 PowerShell 中打开”。

4. 安装依赖

```bash
npm install
```

5. 启动网页

```bash
npm run dev
```

6. 打开浏览器访问终端中显示的网址  
   通常是：

```text
http://localhost:5173/
```

如果终端显示的是其他端口，例如 `http://localhost:5174/`，就打开终端显示的那个网址。

## 方法二：正式预览方式打开

如果希望先生成正式网页文件，可以使用：

```bash
npm run build
```

然后运行：

```bash
npm run preview
```

浏览器访问终端显示的网址即可。

## 注意事项

- 不建议直接双击 `index.html` 打开，因为这是 Vite 项目，推荐通过上面的命令启动。
- 如果已经包含 `node_modules` 文件夹，仍然可以直接运行 `npm run dev`；如果运行失败，再执行一次 `npm install`。
- 网页学习进度保存在当前浏览器本地，换电脑或换浏览器后进度不会自动同步。
- 本项目只用于课程展示和科普学习，不包含真实实验参数、真实操作流程或真实基因编辑指导。

